\documentclass{article}\usepackage[]{graphicx}\usepackage[]{xcolor}
% maxwidth is the original width if it is less than linewidth
% otherwise use linewidth (to make sure the graphics do not exceed the margin)
\makeatletter
\def\maxwidth{ %
  \ifdim\Gin@nat@width>\linewidth
    \linewidth
  \else
    \Gin@nat@width
  \fi
}
\makeatother

\definecolor{fgcolor}{rgb}{0.345, 0.345, 0.345}
\newcommand{\hlnum}[1]{\textcolor[rgb]{0.686,0.059,0.569}{#1}}%
\newcommand{\hlsng}[1]{\textcolor[rgb]{0.192,0.494,0.8}{#1}}%
\newcommand{\hlcom}[1]{\textcolor[rgb]{0.678,0.584,0.686}{\textit{#1}}}%
\newcommand{\hlopt}[1]{\textcolor[rgb]{0,0,0}{#1}}%
\newcommand{\hldef}[1]{\textcolor[rgb]{0.345,0.345,0.345}{#1}}%
\newcommand{\hlkwa}[1]{\textcolor[rgb]{0.161,0.373,0.58}{\textbf{#1}}}%
\newcommand{\hlkwb}[1]{\textcolor[rgb]{0.69,0.353,0.396}{#1}}%
\newcommand{\hlkwc}[1]{\textcolor[rgb]{0.333,0.667,0.333}{#1}}%
\newcommand{\hlkwd}[1]{\textcolor[rgb]{0.737,0.353,0.396}{\textbf{#1}}}%
\let\hlipl\hlkwb

\usepackage{framed}
\makeatletter
\newenvironment{kframe}{%
 \def\at@end@of@kframe{}%
 \ifinner\ifhmode%
  \def\at@end@of@kframe{\end{minipage}}%
  \begin{minipage}{\columnwidth}%
 \fi\fi%
 \def\FrameCommand##1{\hskip\@totalleftmargin \hskip-\fboxsep
 \colorbox{shadecolor}{##1}\hskip-\fboxsep
     % There is no \\@totalrightmargin, so:
     \hskip-\linewidth \hskip-\@totalleftmargin \hskip\columnwidth}%
 \MakeFramed {\advance\hsize-\width
   \@totalleftmargin\z@ \linewidth\hsize
   \@setminipage}}%
 {\par\unskip\endMakeFramed%
 \at@end@of@kframe}
\makeatother

\definecolor{shadecolor}{rgb}{.97, .97, .97}
\definecolor{messagecolor}{rgb}{0, 0, 0}
\definecolor{warningcolor}{rgb}{1, 0, 1}
\definecolor{errorcolor}{rgb}{1, 0, 0}
\newenvironment{knitrout}{}{} % an empty environment to be redefined in TeX

\usepackage{alltt}
\usepackage{graphicx,amsmath,amsfonts,hyperref,titlesec,geometry,esint} 
\renewcommand{\thesection}{\arabic{section}}
\renewcommand{\thesubsection}{(\alph{subsection})}
\renewcommand{\thesubsubsection}{(\alph{subsubsection})}
\geometry{margin=20mm}
\title{Statistical Reasoning HW1}
\author{Alex Melnichenko}
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\begin{document}

\maketitle

\section{}
    \subsection{}
        \begin{align*}
             p(\sigma^{-2}|\mu=0,y_1,\dots,y_n)&\propto p(y_1,\dots,y_n|\mu=0,\sigma^2)p(\mu=0)p(\sigma^{-2})\\
            &=p(y_1,\dots,y_n|\mu=0,\sigma^2)p(\sigma^{-2})\\
            &\propto \prod^n_{i=1}\frac{1}{\sigma}\exp\left(-\frac 12 \sigma^{-2}y_i^2\right)\cdot(\sigma^{-2})^{a-1}\exp(-b\sigma^{-2})\\
            &\propto \sigma^{n+a-1} \exp\left(-\sigma^{-2}\left(\frac 12 \sum_{i=1}^ny_i^2 + b\right)\right)\\
            \implies \sigma^{-2}|\mu=0,y_1,\dots,y_n&\sim GAM\left(n+a, \frac 12 \sum_{i=1}^ny_i^2 + b\right)
        \end{align*}
        
        I inititially attempted to compute the distribution without given $b$ though I couldn't tell if it was proportional to any known distribution, so left it as a proportion below.
        \begin{align*}
                p(\sigma^{-2}|\mu=0,y_1,\dots,y_n)&\propto p(y_1,\dots,y_n|\mu=0,\sigma^2)p(\mu=0)p(\sigma^{-2})\\
                &=p(y_1,\dots,y_n|\mu=0,\sigma^2)p(\sigma^{-2})\\
                &=p(y_1,\dots,y_n|\mu=0,\sigma^2)\int_0^\infty p(\sigma^{-2}|b)p(b)db\\
                &= p(y_1,\dots,y_n|\mu=0,\sigma^2)\int_0^\infty \frac{b^a}{\Gamma(a)}(\sigma^{-2})^{a-1}e^{-\sigma^{-2}b}\frac{\beta^\alpha}{\Gamma(\alpha)}b^{\alpha-1}e^{-b\beta}db\\
                &= p(y_1,\dots,y_n|\mu=0,\sigma^2)\int_0^\infty \frac{b^a}{\Gamma(a)}(\sigma^{-2})^{a-1}e^{-\sigma^{-2}b}\frac{\beta^\alpha}{\Gamma(\alpha)}b^{\alpha-1}e^{-b\beta}db\\
                &= p(y_1,\dots,y_n|\mu=0,\sigma^2)
                \frac{ (\sigma^{-2})^{a-1}}{\Gamma(a)\Gamma(\alpha)}\frac{\Gamma(a+\alpha)}{(\beta+\sigma^{-2})^{a+\alpha}}\int_0^\infty\frac{(\beta+\sigma^{-2})^{a+\alpha}}{\Gamma(a+\alpha)} b^{a+\alpha-1}\exp(-b(\beta+\sigma^{-2}))db\\
                &= \prod_{i=1}^n\frac{1}{\sqrt{2\pi}\sigma}\exp\left(-\frac12\sigma^{-2}y_i^2\right) (\sigma^{-2})^{a-1}
                \frac{1}{\Gamma(a)\Gamma(\alpha)}\frac{\Gamma(a+\alpha)}{(\beta+\sigma^{-2})^{a+\alpha}}\\
                &= \frac{1}{(\sqrt{2\pi})^n}\exp\left(-\frac12\sigma^{-2}\sum_{i=1}^n(y_i)^2\right) (\sigma^{-2})^{\frac n2+a-1}
                \frac{1}{\Gamma(a)\Gamma(\alpha)}\frac{\Gamma(a+\alpha)}{(\beta+\sigma^{-2})^{a+\alpha}}\\
                &\propto  \exp\left(-\frac12\sigma^{-2}\sum_{i=1}^n(y_i)^2\right) (\sigma^{-2})^{\frac n2+a-1}\frac{1}{(\beta+\sigma^{-2})^{a+\alpha}}\\
        \end{align*}
    \subsection{}
        \begin{align*}
            p(b|a,\sigma^{-2})&\propto p(\sigma^{-2}|a,b)\cdot p(a,b)\\
            &=p(\sigma^{-2}|a,b)\cdot p(b)\\
            &= b^{\alpha-1}\exp\left(-b\beta\right)(\sigma^{-2})^{a-1}\exp\left(-\sigma^{-2}b\right)\frac{b^a}{\Gamma(a)}\frac{\beta^\alpha}{\Gamma(\alpha)}\\
            &\propto b^{\alpha-1}\exp\left(-b\beta\right)\exp\left(-\sigma^{-2}b\right)b^a\\
            &= b^{\alpha+a-1}\exp\left(-b(\beta+\sigma^{-2})\right)\\
            \implies b|a,\sigma^{-2} &\sim GAM\left(\alpha + a, \beta + \sigma^{-2}\right)
        \end{align*}
\section{}
    \subsection{}
        \begin{align*}
            P(\theta_1,\dots,\theta_K|y_1,\dots,y_n)&=P(\theta_1,\dots,\theta_K)\cdot P(y_1,\dots,y_n|\theta_1,\dots,\theta_K)\\
            &\propto\prod_{k=1}^K\left(\theta_k^{\alpha_k-
            1}\right)\prod_{j=1}^n\prod_{k=1}^K\left(\theta_k^{I(y_j=k)}\right)\\
            &=\prod_{k=1}^K\left(\theta_k^{\alpha_k-1}\right)\prod_{k=1}^K\left(\theta_k^{\sum_{j=1}^nI(y_j=k)}\right)\\
            &=\prod_{k=1}^K\left(\theta_k^{\alpha_k-1}\theta_k^{\sum_{j=1}^nI(y_j=k)}\right)\\
            &=\prod_{k=1}^K\left(\theta_k^{n_k+\alpha_k-1}\right)\\
            \implies \theta_1,\dots,\theta_K|Y_1,\dots,Y_n&\sim DIR\left({\alpha_1+n_1},\dots, {\alpha_K+n_K}\right)
        \end{align*}
    \subsection{}
        We note that $p(\theta_1,\dots,\theta_K)=\frac{\Gamma\left(\sum^K_{k=1}\alpha_k+n_k\right)}{\prod^K_{k=1}\Gamma(\alpha_k+n_k)}\prod_{k=1}^K\left(\theta_k^{n_k+\alpha_k-1}\right)$ is the density function of a Dirichlet distribution so we have $\idotsint_\Theta\frac{\Gamma\left(\sum^K_{k=1}\alpha_k+n_k\right)}{\prod^K_{k=1}\Gamma(\alpha_k+n_k)}\prod_{k=1}^K\left(\theta_k^{n_k+\alpha_k-1}\right)d\theta_1\dots d\theta_K=1$, which we use getting to the final line.
        \begin{align*}
            p(y_1,\dots,y_n)&=\idotsint_\Theta p(y_1,\dots,y_n|(\theta_1,\dots,\theta_K))p(\theta_1,\dots,\theta_K)d\theta_1\dots d\theta_K\\
            &=\idotsint_\Theta\frac{\Gamma\left(\sum^K_{k=1}\alpha_k\right)}{\prod^K_{k=1}\Gamma(\alpha_k)}\prod_{k=1}^K\left(\theta_k^{n_k+\alpha_k-1}\right)d\theta_1\dots d\theta_K\\
            &=\frac{\Gamma\left(\sum^K_{k=1}\alpha_k\right)}{\prod^K_{k=1}\Gamma(\alpha_k)}\cdot\left(\frac{\Gamma\left(\sum^K_{k=1}\alpha_k+n_k\right)}{\prod^K_{k=1}\Gamma(\alpha_k+n_k)}\right)^{-1}\cdot\idotsint_\Theta\frac{\Gamma\left(\sum^K_{k=1}\alpha_k+n_k\right)}{\prod^K_{k=1}\Gamma(\alpha_k+n_k)}\prod_{k=1}^K\left(\theta_k^{n_k+\alpha_k-1}\right)d\theta_1\dots d\theta_K\\
            &=\frac{\Gamma\left(\sum^K_{k=1}\alpha_k\right)}{\prod^K_{k=1}\Gamma(\alpha_k)}\cdot\left(\frac{\Gamma\left(\sum^K_{k=1}\alpha_k+n_k\right)}{\prod^K_{k=1}\Gamma(\alpha_k+n_k)}\right)^{-1}
        \end{align*}

    \subsection{}
        We have that $Y_{n+1}\sim CAT(\lambda_1,\dots,\lambda_K)$ where $(\Lambda_1,\dots,\Lambda_K)\sim DIR(\alpha_1+n_1,\dots,\alpha_K+n_K)$ by (a). I'm not able to follow the notation like at all if I use $\theta$ here again hence a different variable. We additionally apply (b) to compute.
        \begin{align*}
            p(y_{n+1}=k|y_1,\dots,y_{n})&=\idotsint_\Theta p(y_{n+1}|(\lambda_1,\dots,\lambda_K))p((\lambda_1,\dots,\lambda_K))d\lambda_1\dots d\lambda_K\\
            &=\left(\frac{\Gamma\left(\sum^K_{j=1}\alpha_j+n_j\right)}{\prod^K_{j=1}\Gamma(\alpha_j+n_j)}\cdot\left(\frac{\Gamma\left(1+\sum_{j=1}^K\alpha_j+n_j\right)}{\Gamma(a_k+n_k+1)\prod_{j\ne k}\Gamma(\alpha_j+n_j)}\right)^{-1}\right)\\
            &=  \left(\frac{\Gamma\left(\sum^K_{j=1}\alpha_j+n_j\right)}{\Gamma\left(1+\sum_{j=1}^K\alpha_j+n_j\right)} \cdot   \left(\frac{\prod^K_{j=1}\Gamma(\alpha_j+n_j)}{\Gamma(a_k+n_k+1)\prod_{j\ne k}\Gamma(\alpha_j+n_j)}\right)^{-1}\right)\\
            &= \left(\frac{\Gamma\left(\sum^K_{j=1}\alpha_j+n_j\right)}{\Gamma\left(\sum_{j=1}^K\alpha_j+n_j\right)\cdot\left(\sum_{j=1}^K\alpha_j+n_j\right)} \cdot   \left(\frac{\prod^K_{j=1}\Gamma(\alpha_j+n_j)}{\Gamma(a_k+n_k+1)\prod_{j\ne k}\Gamma(\alpha_j+n_j)}\right)^{-1}\right)\\
            &= \left(\frac{1}{\left(\sum_{j=1}^K\alpha_j+n_j\right)} \cdot   \left(\frac{\prod^K_{j=1}\Gamma(\alpha_j+n_j)}{(a_k+n_k)\prod^K_{j=1}\Gamma(\alpha_j+n_j)}\right)^{-1}\right)\\
            &=\frac{a_k+n_k}{\alpha+n}\\
        \end{align*}
\section{}
        $\oiint\cdot+1-\$

\section{}
% okay look i started this on my laptop with vscode then worked on it here a moment then did it on my desktop in R studio but that was annoying so i moved back to VScode so there isnt any dreaking edit history
\begin{knitrout}
\definecolor{shadecolor}{rgb}{0.969, 0.969, 0.969}\color{fgcolor}\begin{kframe}
\begin{alltt}
\hlcom{## Data}
\hlcom{# SCHOOL s=1}
\hldef{Y1} \hlkwb{<-} \hlkwd{c}\hldef{(}
  \hlnum{2.11}\hldef{,} \hlnum{9.75}\hldef{,} \hlnum{13.88}\hldef{,} \hlnum{11.3}\hldef{,} \hlnum{8.93}\hldef{,} \hlnum{15.66}\hldef{,} \hlnum{16.38}\hldef{,} \hlnum{4.54}\hldef{,} \hlnum{8.86}\hldef{,} \hlnum{11.94}\hldef{,} \hlnum{12.47}\hldef{,} \hlnum{11.11}\hldef{,}
  \hlnum{11.65}\hldef{,} \hlnum{14.53}\hldef{,} \hlnum{9.61}\hldef{,} \hlnum{7.38}\hldef{,} \hlnum{3.34}\hldef{,} \hlnum{9.06}\hldef{,} \hlnum{9.45}\hldef{,} \hlnum{5.98}\hldef{,} \hlnum{7.44}\hldef{,} \hlnum{8.5}\hldef{,} \hlnum{1.55}\hldef{,} \hlnum{11.45}\hldef{,} \hlnum{9.73}
\hldef{)}

\hlcom{# SCHOOL s=2}
\hldef{Y2} \hlkwb{<-} \hlkwd{c}\hldef{(}
  \hlnum{0.29}\hldef{,} \hlnum{1.13}\hldef{,} \hlnum{6.52}\hldef{,} \hlnum{11.72}\hldef{,} \hlnum{6.54}\hldef{,} \hlnum{5.63}\hldef{,} \hlnum{14.59}\hldef{,} \hlnum{11.74}\hldef{,} \hlnum{9.12}\hldef{,} \hlnum{9.43}\hldef{,} \hlnum{10.64}\hldef{,} \hlnum{12.28}\hldef{,}
  \hlnum{9.5}\hldef{,} \hlnum{0.63}\hldef{,} \hlnum{15.35}\hldef{,} \hlnum{5.31}\hldef{,} \hlnum{8.49}\hldef{,} \hlnum{3.04}\hldef{,} \hlnum{3.77}\hldef{,} \hlnum{6.22}\hldef{,} \hlnum{2.14}\hldef{,} \hlnum{6.58}\hldef{,} \hlnum{1.11}
\hldef{)}

\hlcom{# SCHOOL s=3}
\hldef{Y3} \hlkwb{<-} \hlkwd{c}\hldef{(}
  \hlnum{4.33}\hldef{,} \hlnum{7.77}\hldef{,} \hlnum{4.15}\hldef{,} \hlnum{5.64}\hldef{,} \hlnum{7.69}\hldef{,} \hlnum{5.04}\hldef{,} \hlnum{10.01}\hldef{,} \hlnum{13.43}\hldef{,} \hlnum{13.63}\hldef{,} \hlnum{9.9}\hldef{,} \hlnum{5.72}\hldef{,} \hlnum{5.16}\hldef{,} \hlnum{4.33}
  \hldef{,} \hlnum{12.9}\hldef{,} \hlnum{11.27}\hldef{,} \hlnum{6.05}\hldef{,} \hlnum{0.95}\hldef{,} \hlnum{6.02}\hldef{,} \hlnum{12.22}\hldef{,} \hlnum{12.85}
\hldef{)}

\hlcom{## Initialization}
\hldef{Y} \hlkwb{<-} \hlkwd{list}\hldef{(Y1, Y2, Y3)}
\hldef{Ynames} \hlkwb{<-} \hlkwd{list}\hldef{(}\hlsng{"Y1"}\hldef{,} \hlsng{"Y2"}\hldef{,} \hlsng{"Y3"}\hldef{)}

\hlcom{# Inputs:}
\hlcom{# mu0 - first parameter mu of prior on mu prior}
\hlcom{# tau0 - second parameter sigma^2 on mu prior}
\hlcom{# a - first parameter a on sigma^\{-2\} prior}
\hlcom{# b - second parameter b on sigma^\{-2\} prior}
\hlcom{# Yi - samples}
\hlcom{# Outputs}
\hlcom{# gibbsIter - Function taking previous samples mu_t and sigma^2_t and returning}
\hlcom{# next samples mu_\{t+1\} and sigma_\{t+1\}}
\hldef{iterFunc} \hlkwb{<-} \hlkwa{function}\hldef{(}\hlkwc{mu0}\hldef{,} \hlkwc{tau0}\hldef{,} \hlkwc{a}\hldef{,} \hlkwc{b}\hldef{,} \hlkwc{Yi}\hldef{) \{}
  \hldef{gibbsIter} \hlkwb{<-} \hlkwa{function}\hldef{(}\hlkwc{mui}\hldef{,} \hlkwc{sigmai}\hldef{) \{}
    \hldef{n} \hlkwb{<-} \hlkwd{length}\hldef{(Yi)}
    \hldef{muVar} \hlkwb{<-} \hlnum{1} \hlopt{/} \hldef{(n} \hlopt{/} \hldef{sigmai} \hlopt{+} \hlnum{1} \hlopt{/} \hldef{tau0)}
    \hldef{muMean} \hlkwb{<-} \hldef{muVar} \hlopt{*} \hldef{(}\hlkwd{sum}\hldef{(Yi)} \hlopt{/} \hldef{sigmai} \hlopt{+} \hldef{mu0} \hlopt{/} \hldef{tau0)}
    \hldef{muNew} \hlkwb{<-} \hlkwd{rnorm}\hldef{(}\hlnum{1}\hldef{,} \hlkwc{mean} \hldef{= muMean,} \hlkwc{sd} \hldef{=} \hlkwd{sqrt}\hldef{(muVar))}
    \hldef{aTilde} \hlkwb{<-} \hldef{a} \hlopt{+} \hldef{n} \hlopt{/} \hlnum{2}
    \hldef{bTilde} \hlkwb{<-} \hldef{b} \hlopt{+} \hlnum{1} \hlopt{/} \hlnum{2} \hlopt{*} \hlkwd{sum}\hldef{((Yi} \hlopt{-} \hlkwd{rep}\hldef{(muNew, n))}\hlopt{^}\hlnum{2}\hldef{)}
    \hldef{sigmaNew} \hlkwb{<-} \hlnum{1} \hlopt{/} \hlkwd{rgamma}\hldef{(}\hlnum{1}\hldef{,} \hlkwc{shape} \hldef{= aTilde,} \hlkwc{rate} \hldef{= bTilde)}
    \hlkwd{c}\hldef{(muNew, sigmaNew)}
  \hldef{\}}
  \hldef{gibbsIter}
\hldef{\}}
\hlcom{# Inputs:}
\hlcom{# Yi - samples}
\hlcom{# length - Number of samples to generate}
\hlcom{# Outputs}
\hlcom{# (mus, sigmas) - Vector with first length many items being the mu samples and}
\hlcom{# remaining length many items the sigma samples}
\hldef{Gibbs} \hlkwb{<-} \hlkwa{function}\hldef{(}\hlkwc{Yi}\hldef{,} \hlkwc{length}\hldef{) \{}
  \hldef{mu0} \hlkwb{<-} \hlkwd{rnorm}\hldef{(}\hlnum{1}\hldef{,} \hlkwc{mean} \hldef{=} \hlnum{5}\hldef{,} \hlkwc{sd} \hldef{=} \hlkwd{sqrt}\hldef{(}\hlnum{4}\hldef{))}
  \hldef{sigma0} \hlkwb{<-} \hlnum{1} \hlopt{/} \hlkwd{rgamma}\hldef{(}\hlnum{1}\hldef{,} \hlkwc{shape} \hldef{=} \hlnum{1}\hldef{,} \hlkwc{rate} \hldef{=} \hlnum{4}\hldef{)}
  \hldef{iterYi} \hlkwb{<-} \hlkwd{iterFunc}\hldef{(}\hlnum{5}\hldef{,} \hlkwd{sqrt}\hldef{(}\hlnum{4}\hldef{),} \hlnum{1}\hldef{,} \hlnum{4}\hldef{, Yi)}
  \hldef{mus} \hlkwb{<-} \hlkwd{rep}\hldef{(}\hlnum{1}\hldef{, length)}
  \hldef{sigmas} \hlkwb{<-} \hlkwd{rep}\hldef{(}\hlnum{1}\hldef{, length)}
  \hldef{mus[}\hlnum{1}\hldef{]} \hlkwb{<-} \hldef{mu0}
  \hldef{sigmas[}\hlnum{1}\hldef{]} \hlkwb{<-} \hldef{sigma0}
  \hlkwd{iterYi}\hldef{(mu0, sigma0)}
  \hlkwa{for} \hldef{(i} \hlkwa{in} \hlnum{2}\hlopt{:}\hldef{length) \{}
    \hldef{t} \hlkwb{<-} \hlkwd{iterYi}\hldef{(mus[i} \hlopt{-} \hlnum{1}\hldef{], sigmas[i} \hlopt{-} \hlnum{1}\hldef{])}
    \hldef{mus[i]} \hlkwb{<-} \hldef{t[}\hlnum{1}\hldef{]}
    \hldef{sigmas[i]} \hlkwb{<-} \hldef{t[}\hlnum{2}\hldef{]}
  \hldef{\}}
  \hlkwd{c}\hldef{(mus, sigmas)}
\hldef{\}}

\hlcom{# Perform samples}
\hldef{length} \hlkwb{<-} \hlnum{20000}
\hldef{a1} \hlkwb{<-} \hlnum{1}
\hldef{b1} \hlkwb{<-} \hldef{length}
\hldef{b2} \hlkwb{<-} \hlnum{2} \hlopt{*} \hldef{length}
\hldef{a2} \hlkwb{<-} \hldef{length} \hlopt{+} \hlnum{1}
\hldef{mus} \hlkwb{<-} \hlkwd{list}\hldef{(}\hlnum{1}\hldef{,} \hlnum{2}\hldef{,} \hlnum{3}\hldef{)}
\hldef{sigmas} \hlkwb{<-} \hlkwd{list}\hldef{(}\hlnum{1}\hldef{,} \hlnum{2}\hldef{,} \hlnum{3}\hldef{)}
\hlkwa{for} \hldef{(i} \hlkwa{in} \hlnum{1}\hlopt{:}\hlnum{3}\hldef{) \{}
  \hldef{samples} \hlkwb{<-} \hlkwd{Gibbs}\hldef{(Y[[i]], length)}
  \hldef{mus[[i]]} \hlkwb{<-} \hldef{samples[a1}\hlopt{:}\hldef{b1]}
  \hldef{sigmas[[i]]} \hlkwb{<-} \hldef{samples[a2}\hlopt{:}\hldef{b2]}
\hldef{\}}

\hlcom{# Permutations we use for (d) and (e)}
\hlcom{# I looked it up and there is a package for this but this is only six rows}
\hldef{permutations} \hlkwb{<-} \hlkwd{matrix}\hldef{(}\hlnum{0}\hldef{,} \hlnum{6}\hldef{,} \hlnum{3}\hldef{)}
\hldef{permutations[}\hlnum{1}\hldef{,} \hlnum{1}\hlopt{:}\hlnum{3}\hldef{]} \hlkwb{<-} \hlkwd{c}\hldef{(}\hlnum{1}\hldef{,} \hlnum{2}\hldef{,} \hlnum{3}\hldef{)}
\hldef{permutations[}\hlnum{2}\hldef{,} \hlnum{1}\hlopt{:}\hlnum{3}\hldef{]} \hlkwb{<-} \hlkwd{c}\hldef{(}\hlnum{1}\hldef{,} \hlnum{3}\hldef{,} \hlnum{2}\hldef{)}
\hldef{permutations[}\hlnum{3}\hldef{,} \hlnum{1}\hlopt{:}\hlnum{3}\hldef{]} \hlkwb{<-} \hlkwd{c}\hldef{(}\hlnum{2}\hldef{,} \hlnum{1}\hldef{,} \hlnum{3}\hldef{)}
\hldef{permutations[}\hlnum{4}\hldef{,} \hlnum{1}\hlopt{:}\hlnum{3}\hldef{]} \hlkwb{<-} \hlkwd{c}\hldef{(}\hlnum{2}\hldef{,} \hlnum{3}\hldef{,} \hlnum{1}\hldef{)}
\hldef{permutations[}\hlnum{5}\hldef{,} \hlnum{1}\hlopt{:}\hlnum{3}\hldef{]} \hlkwb{<-} \hlkwd{c}\hldef{(}\hlnum{3}\hldef{,} \hlnum{1}\hldef{,} \hlnum{2}\hldef{)}
\hldef{permutations[}\hlnum{6}\hldef{,} \hlnum{1}\hlopt{:}\hlnum{3}\hldef{]} \hlkwb{<-} \hlkwd{c}\hldef{(}\hlnum{3}\hldef{,} \hlnum{2}\hldef{,} \hlnum{1}\hldef{)}


\hlcom{## (a)}
\hlkwa{for} \hldef{(i} \hlkwa{in} \hlnum{1}\hlopt{:}\hlnum{3}\hldef{) \{}
  \hldef{muMean} \hlkwb{<-} \hlkwd{mean}\hldef{(mus[[i]])}
  \hldef{sigmaMean} \hlkwb{<-} \hlkwd{mean}\hldef{(sigmas[[i]])}
  \hlkwd{print}\hldef{(}\hlkwd{paste}\hldef{(}\hlsng{"The posterior mean for mu of"}\hldef{, Ynames[[i]],} \hlsng{"is"}\hldef{, muMean))}
  \hlkwd{print}\hldef{(}\hlkwd{paste}\hldef{(}\hlsng{"The posterior mean for sigma^2 of"}\hldef{, Ynames[[i]],} \hlsng{"is"}\hldef{, sigmaMean)}
  \hldef{)}
\hldef{\}}
\end{alltt}
\begin{verbatim}
## [1] "The posterior mean for mu of Y1 is 8.37153954399216"
## [1] "The posterior mean for sigma^2 of Y1 is 16.5007601742381"
## [1] "The posterior mean for mu of Y2 is 6.41838689526267"
## [1] "The posterior mean for sigma^2 of Y2 is 20.5232504323136"
## [1] "The posterior mean for mu of Y3 is 7.15752397947134"
## [1] "The posterior mean for sigma^2 of Y3 is 15.1952568219661"
\end{verbatim}
\begin{alltt}
\hlcom{## (b)}
\hlkwa{for} \hldef{(i} \hlkwa{in} \hlnum{1}\hlopt{:}\hlnum{3}\hldef{) \{}
  \hldef{muQuant} \hlkwb{<-} \hlkwd{quantile}\hldef{(mus[[i]],} \hlkwc{probs} \hldef{=} \hlkwd{c}\hldef{(}\hlnum{0.025}\hldef{,} \hlnum{0.975}\hldef{))}
  \hldef{sigmaQuant} \hlkwb{<-} \hlkwd{quantile}\hldef{(sigmas[[i]],} \hlkwc{probs} \hldef{=} \hlkwd{c}\hldef{(}\hlnum{0.025}\hldef{,} \hlnum{0.975}\hldef{))}
  \hlkwd{print}\hldef{(}\hlkwd{paste}\hldef{(}
    \hlsng{"The empirical 95% confidence interval for mu of"}\hldef{, Ynames[[i]],}
    \hlsng{"is("}\hldef{, muQuant[}\hlnum{1}\hldef{],} \hlsng{","}\hldef{, muQuant[}\hlnum{2}\hldef{],} \hlsng{")"}
  \hldef{))}
  \hlkwd{print}\hldef{(}\hlkwd{paste}\hldef{(}
    \hlsng{"The empirical 95% confidence interval for sigma^2 of"}\hldef{,}
    \hldef{Ynames[[i]],} \hlsng{"is ("}\hldef{, sigmaQuant[}\hlnum{1}\hldef{],} \hlsng{","}\hldef{, sigmaQuant[}\hlnum{2}\hldef{],} \hlsng{")"}
  \hldef{))}
\hldef{\}}
\end{alltt}
\begin{verbatim}
## [1] "The empirical 95% confidence interval for mu of Y1 is( 6.83355671274148 , 9.73211055217593 )"
## [1] "The empirical 95% confidence interval for sigma^2 of Y1 is ( 9.2373888642346 , 29.2152700323206 )"
## [1] "The empirical 95% confidence interval for mu of Y2 is( 4.85173789870027 , 7.91677692218941 )"
## [1] "The empirical 95% confidence interval for sigma^2 of Y2 is ( 11.4912407849963 , 36.5299422767055 )"
## [1] "The empirical 95% confidence interval for mu of Y3 is( 5.58592916404238 , 8.6044994144952 )"
## [1] "The empirical 95% confidence interval for sigma^2 of Y3 is ( 8.02663143318418 , 28.5083091969154 )"
\end{verbatim}
\begin{alltt}
\hlcom{## (c)}
\hlkwa{for} \hldef{(i} \hlkwa{in} \hlnum{1}\hlopt{:}\hlnum{6}\hldef{) \{}
  \hldef{perm} \hlkwb{<-} \hldef{permutations[i,} \hlnum{1}\hlopt{:}\hlnum{3}\hldef{]}
  \hldef{t} \hlkwb{<-} \hlnum{0}
  \hlcom{# Check and tally for which samples this holds true}
  \hlkwa{for} \hldef{(j} \hlkwa{in} \hlnum{1}\hlopt{:}\hldef{length) \{}
    \hlkwa{if} \hldef{(mus[[perm[}\hlnum{1}\hldef{]]][j]} \hlopt{<} \hldef{mus[[perm[}\hlnum{2}\hldef{]]][j]) \{}
      \hlkwa{if} \hldef{(mus[[perm[}\hlnum{2}\hldef{]]][j]} \hlopt{<} \hldef{mus[[perm[}\hlnum{3}\hldef{]]][j]) \{}
        \hldef{t} \hlkwb{<-} \hldef{t} \hlopt{+} \hlnum{1}
      \hldef{\}}
    \hldef{\}}
  \hldef{\}}
  \hlcom{# Normalize and print}
  \hldef{t} \hlkwb{<-} \hldef{t} \hlopt{/} \hldef{length}
  \hlkwd{cat}\hldef{(}\hlkwd{sprintf}\hldef{(}\hlsng{"P(mu%d < mu%d < mu%d) = %f\textbackslash{}n"}\hldef{, perm[}\hlnum{1}\hldef{], perm[}\hlnum{2}\hldef{], perm[}\hlnum{3}\hldef{], t))}
\hldef{\}}
\end{alltt}
\begin{verbatim}
## P(mu1 < mu2 < mu3) = 0.012900
## P(mu1 < mu3 < mu2) = 0.007550
## P(mu2 < mu1 < mu3) = 0.106300
## P(mu2 < mu3 < mu1) = 0.633150
## P(mu3 < mu1 < mu2) = 0.016000
## P(mu3 < mu2 < mu1) = 0.224100
\end{verbatim}
\begin{alltt}
\hlcom{## (d)}
\hlcom{# This is an application of the indirect Monte Carlo approximation from lecture}
\hlcom{# 5.}
\hlkwa{for} \hldef{(i} \hlkwa{in} \hlnum{1}\hlopt{:}\hlnum{6}\hldef{) \{}
  \hldef{perm} \hlkwb{<-} \hldef{permutations[i,} \hlnum{1}\hlopt{:}\hlnum{3}\hldef{]}
  \hldef{t} \hlkwb{<-} \hlnum{0}
  \hlkwa{for} \hldef{(j} \hlkwa{in} \hlnum{1}\hlopt{:}\hldef{length) \{}
  \hlcom{# Very similar to (c) though we sample each distribution once}
    \hldef{samples} \hlkwb{<-} \hlnum{1}\hlopt{:}\hlnum{3}
    \hlkwa{for} \hldef{(k} \hlkwa{in} \hlnum{1}\hlopt{:}\hlnum{3}\hldef{) \{}
      \hldef{mu} \hlkwb{<-} \hldef{mus[[perm[k]]][j]}
      \hldef{sigma} \hlkwb{<-} \hldef{sigmas[[perm[k]]][j]}
      \hldef{samples[k]} \hlkwb{<-} \hlkwd{rnorm}\hldef{(}\hlnum{1}\hldef{, mu,} \hlkwd{sqrt}\hldef{(sigma))}
    \hldef{\}}
    \hlkwa{if} \hldef{(samples[}\hlnum{1}\hldef{]} \hlopt{<} \hldef{samples[}\hlnum{2}\hldef{]) \{}
      \hlkwa{if} \hldef{(samples[}\hlnum{2}\hldef{]} \hlopt{<} \hldef{samples[}\hlnum{3}\hldef{]) \{}
        \hldef{t} \hlkwb{<-} \hldef{t} \hlopt{+} \hlnum{1}
      \hldef{\}}
    \hldef{\}}
  \hldef{\}}
  \hldef{t} \hlkwb{<-} \hldef{t} \hlopt{/} \hldef{length}
  \hlkwd{cat}\hldef{(}\hlkwd{sprintf}\hldef{(}\hlsng{"P(~Y%d < ~Y%d < ~Y%d) = %f\textbackslash{}n"}\hldef{, perm[}\hlnum{1}\hldef{], perm[}\hlnum{2}\hldef{], perm[}\hlnum{3}\hldef{], t))}
\hldef{\}}
\end{alltt}
\begin{verbatim}
## P(~Y1 < ~Y2 < ~Y3) = 0.113750
## P(~Y1 < ~Y3 < ~Y2) = 0.115850
## P(~Y2 < ~Y1 < ~Y3) = 0.189150
## P(~Y2 < ~Y3 < ~Y1) = 0.248500
## P(~Y3 < ~Y1 < ~Y2) = 0.141150
## P(~Y3 < ~Y2 < ~Y1) = 0.192900
\end{verbatim}
\begin{alltt}
\hlcom{## (e)}
\hldef{t} \hlkwb{<-} \hlnum{0}
\hlkwa{for} \hldef{(i} \hlkwa{in} \hlnum{1}\hlopt{:}\hldef{length) \{}
  \hlkwa{if} \hldef{(mus[[}\hlnum{1}\hldef{]][i]} \hlopt{>} \hldef{mus[[}\hlnum{2}\hldef{]][i]) \{}
    \hlkwa{if} \hldef{(mus[[}\hlnum{1}\hldef{]][i]} \hlopt{>} \hldef{mus[[}\hlnum{3}\hldef{]][i]) \{}
      \hldef{t} \hlkwb{=} \hldef{t} \hlopt{+} \hlnum{1}
    \hldef{\}}
  \hldef{\}}
\hldef{\}}
\hldef{t} \hlkwb{<-} \hldef{t} \hlopt{/} \hldef{length}
\hlkwd{cat}\hldef{(}\hlkwd{sprintf}\hldef{(}\hlsng{"P(mu1 < mu2, mu1  < mu3) = %f\textbackslash{}n"}\hldef{, t))}
\end{alltt}
\begin{verbatim}
## P(mu1 < mu2, mu1  < mu3) = 0.857250
\end{verbatim}
\begin{alltt}
\hlcom{## (f)}
\hldef{t} \hlkwb{<-} \hlnum{0}
\hlkwa{for} \hldef{(j} \hlkwa{in} \hlnum{1}\hlopt{:}\hldef{length) \{}
  \hldef{samples} \hlkwb{<-} \hlnum{1}\hlopt{:}\hlnum{3}
  \hlkwa{for} \hldef{(k} \hlkwa{in} \hlnum{1}\hlopt{:}\hlnum{3}\hldef{) \{}
    \hldef{mu} \hlkwb{<-} \hldef{mus[[k]][j]}
    \hldef{sigma} \hlkwb{<-} \hldef{sigmas[[k]][j]}
    \hldef{samples[k]} \hlkwb{<-} \hlkwd{rnorm}\hldef{(}\hlnum{1}\hldef{, mu,} \hlkwd{sqrt}\hldef{(sigma))}
  \hldef{\}}
  \hlkwa{if} \hldef{(samples[}\hlnum{1}\hldef{]} \hlopt{>} \hldef{samples[}\hlnum{2}\hldef{]) \{}
    \hlkwa{if} \hldef{(samples[}\hlnum{2}\hldef{]} \hlopt{>} \hldef{samples[}\hlnum{3}\hldef{]) \{}
      \hldef{t} \hlkwb{<-} \hldef{t} \hlopt{+} \hlnum{1}
    \hldef{\}}
  \hldef{\}}
\hldef{\}}
\hldef{t} \hlkwb{<-} \hldef{t} \hlopt{/} \hldef{length}
\hlkwd{cat}\hldef{(}\hlkwd{sprintf}\hldef{(}\hlsng{"P(~Y1<~Y2, ~Y1<~Y3) = %f\textbackslash{}n"}\hldef{, t))}
\end{alltt}
\begin{verbatim}
## P(~Y1<~Y2, ~Y1<~Y3) = 0.198450
\end{verbatim}
\end{kframe}
\end{knitrout}
\end{document}
